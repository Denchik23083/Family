@page "/parents"

<div class="wrapper">
    @foreach (var parent in Parents)
    {
        <div class="block m-3" @onclick="() => RouteId(parent.Id)">
            <IndividualParentsComponent Parent="parent"/>
        </div>
    }
</div>
