@page "/children"

<div class="wrapper">
    @foreach (var child in Children)
    {
        <div class="from_all_click" @onclick="() => RouteToChild(child.Id)">
            <IndividualChildrenComponent Child="child"/>
        </div>
    }
</div>
